{
  "name": "dbq",
  "version": "0.2.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "bun run build:release",
    "build:auth": "swiftc bin/confirm-query.swift -o dist/package/bin/dbq-confirm",
    "build:cli": "bun build --compile --outfile dist/package/bin/dbq src/index.ts",
    "build:release": "bun scripts/release.ts build",
    "check": "bun run format && bun run lint && bun run typecheck",
    "format": "oxfmt --write .",
    "install:local": "./install-local.sh",
    "lint": "oxlint . --deny-warnings",
    "release": "bun scripts/release.ts create",
    "release:homebrew": "bun scripts/release.ts homebrew",
    "start": "bun run src/index.ts",
    "typecheck": "tsgo --noEmit"
  },
  "dependencies": {
    "@effect/cli": "^0.75.2",
    "@effect/platform": "^0.96.1",
    "@effect/platform-bun": "^0.90.0",
    "effect": "^3.21.2"
  },
  "devDependencies": {
    "@types/bun": "^1.3.14",
    "@typescript/native-preview": "^7.0.0-dev.20260604.1",
    "oxfmt": "^0.53.0",
    "oxlint": "^1.68.0",
    "oxlint-plugin-eslint": "^1.68.0"
  }
}
